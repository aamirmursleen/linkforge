import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "LinkForge - Modern Link Management Platform",
  description: "Create short links, QR codes, and track analytics. The all-in-one platform for modern link management.",
  keywords: ["link shortener", "QR codes", "analytics", "URL management", "branded links"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}
